# Local Development Settings Override
# Copy this to .env.local and it will force SQLite usage

DEBUG=True
DATABASE_URL=
DB_HOST=
DB_NAME=db.sqlite3

# This forces the settings to use SQLite instead of remote PostgreSQL
# Result: Sub-10ms query times instead of 200-500ms
